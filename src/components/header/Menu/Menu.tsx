import './menu.css'

const Menu = () => {
  return (
    <div>
        <button className="menu-btn">
            MENU <span className="icon">=</span>
        </button>

    </div>
  );
}

export default Menu